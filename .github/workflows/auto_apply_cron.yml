name: 24/7 Cloud Auto-Apply Campaign Runner (DISABLED)

on:
  workflow_dispatch:

jobs:
  run-campaign:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Automated cloud cron runner has been permanently disabled."